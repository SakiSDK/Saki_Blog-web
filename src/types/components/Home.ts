export interface PictureCardData {
  tip: string;
  content: string;
}
export interface AboutCardData {
  name: string;
  detail: string;
  content: string;
  innerContent: string;
  tip: string;
  title: string;
  emoji: string;
  btns: any
}